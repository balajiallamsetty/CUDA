# Public frontend config only (bundled into the client).
# Leave empty for local Vite dev (uses /api proxy to the local API).
# On Vercel Production set:
#   VITE_API_URL=https://cuda-v8ms.onrender.com
VITE_API_URL=
